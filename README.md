Test WP Theme coded with ACF PRO

## Instructions

Install all npm and composer modules and libraries!

NPM:

`npm install`

`npm run dev/build` - for running Tailwind CSS compilation

COMPOSER:

`composer install`